---
stepsCompleted:
  - 1
  - 2
  - 3
  - 4
inputDocuments:
  - _bmad-output/planning-artifacts/prds/prd-Athena-2026-07-08/prd.md
  - _bmad-output/planning-artifacts/architecture/architecture-Athena-2026-07-08/ARCHITECTURE-SPINE.md
---

# Athena - Epic Breakdown

## Overview

This document provides the complete epic and story breakdown for Athena, decomposing the requirements from the PRD, UX Design if it exists, and Architecture requirements into implementable stories.

## Requirements Inventory

### Functional Requirements

FR-1: Athena can launch as a branded custom Theia-based product shell on the desktop-first proof path.
FR-2: Athena can ship with a curated bundled capability set for repository opening, Athena editing, diagnostics, semantic inspection, and basic workbench operation.
FR-3: Athena keeps the Theia product shell downstream of kernel and runtime semantic authority.
FR-4: Athena can open an existing Engineering Repository into an active Repository Session.
FR-5: Athena can create a new Engineering Repository through the workbench.
FR-6: Athena can keep one active runtime-backed Repository Session aligned with runtime ownership while the workbench is open.
FR-7: Athena can surface authored-source diagnostics in the workbench through Athena LSP.
FR-8: Athena can provide core language authoring support through Athena LSP inside the Theia workbench.
FR-9: Athena can host language tooling on top of Athena-owned incremental semantic service state rather than UI-owned AST authority.
FR-10: Athena can provide a professional multi-panel workbench baseline for engineering authoring and inspection.
FR-11: Athena can route core workbench commands and views through explicit product boundaries.
FR-12: Athena can surface read-only semantic inspection beside source editing in the same workbench.
FR-13: Athena can deliver the primary M4 product proof on the desktop-first path.
FR-14: Athena can preserve Theia-based growth toward future browser and collaboration surfaces without requiring them in M4.

### NonFunctional Requirements

NFR-1: Athena must be legible as a custom Theia-based product, not as a loose assembly of demos.
NFR-2: Kernel and runtime remain the semantic authorities; the workbench remains downstream.
NFR-3: Repository session state, diagnostics, and semantic inspection paths must remain inspectable enough for architecture and implementation debugging.
NFR-4: The milestone must be buildable and runnable on the current local development environment with Java 25 and the current workstation constraints.
NFR-5: M4 must not block the later repository/package graph milestone or the later semantic-SCM milestone.

### Additional Requirements

- Athena is based on Eclipse Theia as the foundation for this milestone and must be treated as a custom Theia product, not as a VS Code extension product.
- Introduce a dedicated `ide/` group for the Athena IDE path with a minimal seed of `ide/theia-product`, `ide/theia-frontend`, `ide/theia-backend`, and `ide/lsp`.
- Keep ownership separate: Theia frontend owns presentation and panel composition, Theia backend owns product startup and process orchestration, and Athena LSP owns language services, repository session authority, diagnostics, navigation, and semantic inspection methods.
- The Athena LSP server is the only semantic entry point for the IDE path and must embed the existing JVM runtime, compiler, validation, and plugin-hosting stack.
- One product window hosts one active Engineering Repository and one active Repository Session in M4; multi-root repository support is deferred.
- The authoritative active Repository Session is owned by the LSP-embedded runtime, not by frontend-only or backend-only state.
- Semantic inspection remains read-only and protocol-driven in M4 unless a path already exists through runtime-owned commands inherited from earlier milestones.
- `ide/theia-product` defines the curated bundled capability set; marketplace-style extension sprawl and open-ended user extension management are out of scope.
- M4 repository bootstrap must stay deliberately light and must not hard-code final `athena.yaml`, `athena.lock`, dependency-resolution, or semantic package-graph rules because those belong to M5.
- Cross-language boundaries remain protocol boundaries; `ide/` owns Node/TypeScript/Theia product modules while `kernel/` remains the JVM semantic stack.
- The known-good local reference for Theia product wiring is `D:/Aaron/workspace/projects/2026/eos/theia-ide`, which has already been debugged, built, and run successfully on this workstation.
- Existing `apps/cli`, `apps/desktop-viewer`, and `ui/compose-workbench` remain secondary shells or proof surfaces during M4 rather than the home of the primary IDE product.
- M4 must preserve a later graphical semantic-projection path under the same Theia product boundary, but graphical editing and graph-server implementation remain out of scope for this milestone.
- Future graphical views must stay downstream of canonical semantic state and projection metadata rather than becoming canvas-owned engineering truth.

### UX Design Requirements

None identified. The available UX design contract is for Athena M1 and was excluded from this M4 extraction to avoid pulling an earlier milestone's UI contract into the Theia platform proof.

### FR Coverage Map

FR-1: Epic 1 - custom Athena Theia product launch
FR-2: Epic 1 - curated bundled capability set
FR-3: Epic 3 - workbench remains downstream of kernel and runtime authority
FR-4: Epic 1 - open existing Engineering Repository
FR-5: Epic 1 - create new Engineering Repository
FR-6: Epic 1 - maintain one active Repository Session
FR-7: Epic 2 - diagnostics through Athena LSP
FR-8: Epic 2 - core language tooling through Athena LSP
FR-9: Epic 2 - incremental semantic service model for tooling
FR-10: Epic 3 - professional multi-panel workbench baseline
FR-11: Epic 3 - command and view orchestration
FR-12: Epic 3 - semantic inspection beside source editing
FR-13: Epic 1 - desktop-first primary proof path
FR-14: Epic 3 - preserve Theia-based growth toward future browser, collaboration, and graphical projection surfaces

## Epic List

### Epic 1: Launch And Open Athena As A Real IDE
A platform engineer or engineer can launch the Athena Theia product, enter an Athena-owned desktop shell, and open or create one active Engineering Repository for work.
**FRs covered:** FR-1, FR-2, FR-4, FR-5, FR-6, FR-13

### Epic 2: Edit Athena Source With Serious Language Tooling
An engineer can edit Athena-authored source in the workbench and receive diagnostics, navigation, and other core authoring support through Athena LSP.
**FRs covered:** FR-7, FR-8, FR-9

### Epic 3: Work Inside A Professional Engineering Workbench
An engineer can work inside a structured Athena workbench with repository navigation, command and view orchestration, and read-only semantic inspection beside source editing while preserving semantic authority boundaries and leaving room for later graphical projection surfaces.
**FRs covered:** FR-3, FR-10, FR-11, FR-12, FR-14

## Epic 1: Launch And Open Athena As A Real IDE

A platform engineer or engineer can launch the Athena Theia product, enter an Athena-owned desktop shell, and open or create one active Engineering Repository for work.

### Story 1.1: Establish The `ide/` Product Group From The Known-Good Theia Baseline

As a platform engineer,
I want Athena to introduce a dedicated `ide/` product group using the known-good local Theia sample as the structural baseline,
So that the main IDE path has a maintainable foundation instead of being scattered across legacy app shells.

**Acceptance Criteria:**

**Given** the current Athena workspace and the known-good local reference at `D:/Aaron/workspace/projects/2026/eos/theia-ide`
**When** the first M4 product modules are introduced
**Then** Athena adds an `ide/` group with the minimal seed `theia-product`, `theia-frontend`, `theia-backend`, and `lsp`
**And** the module shape follows the proven Theia sample closely enough to avoid inventing a new product layout from scratch

**Given** the new `ide/` group exists
**When** ownership boundaries are reviewed
**Then** `apps/cli`, `apps/desktop-viewer`, and `ui/compose-workbench` remain secondary proof shells
**And** the primary IDE path is clearly centered under `ide/`

### Story 1.2: Launch A Branded Athena Theia Desktop Shell With A Curated Capability Set

As an engineer,
I want Athena to launch as a branded Theia desktop product with an Athena-owned capability set,
So that the product feels like a real engineering IDE instead of a generic upstream shell.

**Acceptance Criteria:**

**Given** the `ide/` product modules exist
**When** the Athena desktop product is launched
**Then** it opens as a branded Athena shell rather than a generic upstream Theia demo
**And** product identity, top-level shell flow, and core workbench framing are controlled by Athena

**Given** the first product shell is composed
**When** bundled capabilities are selected
**Then** only the capabilities needed for repository opening, source editing, diagnostics, semantic inspection, console visibility, and workbench operation are shipped
**And** marketplace-style extension sprawl is not part of the M4 proof

### Story 1.3: Open One Existing Engineering Repository Into An Active Repository Session

As an engineer,
I want to open one existing Engineering Repository into the Athena product,
So that I can begin work in a real repository-backed session instead of a demo-only shell.

**Acceptance Criteria:**

**Given** the Athena Theia product is running
**When** I choose an existing Engineering Repository from the open flow
**Then** Athena opens that repository root and activates one Repository Session for the product window
**And** the session authority is created in the LSP-embedded runtime rather than in frontend-only state

**Given** one Repository Session is already active
**When** I open another repository in the same window
**Then** the previous session is replaced according to the single-session M4 rule
**And** multi-root repository behavior is not introduced implicitly

### Story 1.4: Create A New Engineering Repository From The Athena Welcome Flow

As an engineer,
I want Athena to create a new Engineering Repository from the product shell,
So that I can start new work without leaving the IDE or waiting for M5 repository contracts.

**Acceptance Criteria:**

**Given** the Athena welcome flow is visible
**When** I create a new Engineering Repository
**Then** Athena creates the minimum repository bootstrap structure required for the M4 proof
**And** the new repository can be activated immediately as the active Repository Session

**Given** the repository bootstrap is created in M4
**When** the created files and folders are reviewed
**Then** the structure is intentionally light and suitable for current Athena source conventions
**And** it does not freeze final `athena.yaml`, `athena.lock`, or package-resolution rules that belong to M5

### Story 1.5: Stabilize The Desktop-First Build, Launch, And Verification Path For The Product Shell

As a platform owner,
I want the Athena Theia product to build and launch predictably on the current workstation,
So that M4 can close with a trustworthy desktop-first proof path.

**Acceptance Criteria:**

**Given** the Athena Theia product modules and repository flows are in place
**When** the documented desktop build and launch steps are executed on the current workstation
**Then** the product builds and starts successfully through a deterministic local path
**And** the proof remains compatible with the current workstation constraints and Java 25 environment

**Given** the desktop-first path is documented and verified
**When** the product is explained as an M4 milestone outcome
**Then** it is clear that M4 has proven desktop-first product embodiment
**And** browser-first expansion remains deferred without blocking future Theia-based growth

## Epic 2: Edit Athena Source With Serious Language Tooling

An engineer can edit Athena-authored source in the workbench and receive diagnostics, navigation, and other core authoring support through Athena LSP.

### Story 2.1: Bootstrap Athena LSP As The Sole Semantic Entry Point For The IDE

As a language-tooling engineer,
I want Athena LSP to be wired into the Theia product as the only semantic entry point,
So that the IDE reaches kernel and runtime meaning through one governed protocol boundary.

**Acceptance Criteria:**

**Given** the Theia product modules exist
**When** the first LSP integration is introduced
**Then** Athena provides an `ide/lsp` module that embeds the existing JVM runtime, compiler, validation, and plugin-hosting stack
**And** neither the Theia frontend nor backend calls `kernel/*` directly for semantic state

**Given** the Theia product and the Athena LSP server are both running
**When** the workbench opens an Athena-authored source file
**Then** the frontend communicates with semantic services through standard or Athena-namespaced LSP methods
**And** the IDE semantic path is visibly `frontend -> LSP -> runtime/compiler`

### Story 2.2: Surface Authored-Source Diagnostics Through Athena LSP

As an engineer,
I want authored-source diagnostics to appear in the editor and workbench problem surfaces,
So that I can immediately see syntax and semantic issues while editing.

**Acceptance Criteria:**

**Given** an active Repository Session contains Athena-authored source
**When** I open or edit a source file in the workbench
**Then** Athena LSP publishes diagnostics derived from Athena-owned parsing, semantic analysis, and validation
**And** the diagnostics are visible in both the editor and Problems-style workbench surfaces

**Given** diagnostics are produced through Athena LSP
**When** I inspect their origin
**Then** they remain traceable to the existing JVM semantic stack
**And** the Theia client does not invent an independent diagnostic engine

### Story 2.3: Add Core Language Navigation And Authoring Support Through Athena LSP

As an engineer,
I want core language tooling such as completion and navigation in the workbench,
So that Athena source authoring behaves like a serious programmable environment.

**Acceptance Criteria:**

**Given** Athena LSP is wired into the workbench
**When** I use the minimum approved M4 language features on Athena-authored source
**Then** the product supports a serious baseline such as completion, document symbols, go-to-definition, or references
**And** those features are available inside the Theia workbench rather than only through CLI tooling

**Given** the first navigation and authoring features are implemented
**When** the feature boundary is reviewed
**Then** the implementation remains small enough for M4
**And** richer language behavior can be added later without changing the semantic authority boundary

### Story 2.4: Keep Diagnostics And Navigation Stable Across Repeated Editing

As an engineer,
I want diagnostics and navigation to stay coherent while I repeatedly edit the same Athena source file,
So that the workbench behaves like a serious editor session instead of recomputing from scratch in a fragile way.

**Acceptance Criteria:**

**Given** an Athena-authored source file is edited repeatedly in one active Repository Session
**When** I make consecutive edits and request diagnostics or navigation results after each change
**Then** Athena LSP returns results that stay aligned with the latest saved or in-memory document state
**And** the workbench does not fall back to a disposable frontend-owned semantic model between requests

**Given** a file has already been analyzed in the current Repository Session
**When** I request diagnostics, symbols, completion, or navigation again after another edit
**Then** Athena LSP reuses governed session state for the repeated request path
**And** the repeated request remains continuous from the user's perspective instead of behaving like a brand-new cold session every time

**Given** repeated edits introduce and then resolve an authored-source problem
**When** I inspect diagnostics and navigation after each edit
**Then** stale issues do not remain visible after they are resolved
**And** language-service results remain traceable to Athena-owned semantic service state rather than UI-owned AST authority

## Epic 3: Work Inside A Professional Engineering Workbench

An engineer can work inside a structured Athena workbench with repository navigation, command and view orchestration, and read-only semantic inspection beside source editing while preserving semantic authority boundaries.

### Story 3.1: Compose A Professional Multi-Panel Athena Workbench

As an engineer,
I want Athena to provide a serious multi-panel workbench layout,
So that I can work in an IDE-shaped environment rather than a bare editor window.

**Acceptance Criteria:**

**Given** the Athena Theia product is running with an active Repository Session
**When** the main workbench is shown
**Then** it includes an editor area plus repository navigation and diagnostics-facing panels
**And** the structure reads as a professional IDE baseline rather than a temporary demo shell

**Given** the workbench baseline is composed
**When** future product growth is considered
**Then** the layout leaves room for semantic inspection, console visibility, and later engineering-facing panes
**And** the M4 composition leaves room for later graphical semantic-projection surfaces under the same shell
**And** the M4 composition does not hard-code a throwaway shell that would need replacement in M5 or M6

### Story 3.2: Route Core Athena Commands And Views Through Explicit Product Boundaries

As an engineer,
I want core Athena commands and views to be deliberately wired into the product shell,
So that repository actions and workbench behavior remain Athena-owned rather than raw upstream defaults.

**Acceptance Criteria:**

**Given** the workbench baseline exists
**When** I use core product actions such as open repository, create repository, or reveal diagnostic and semantic views
**Then** those actions are available through explicit Athena command and menu entry points
**And** the view composition is controlled by Athena rather than by uncurated default shell behavior

**Given** Theia frontend and backend ownership is split
**When** command and view wiring is reviewed
**Then** presentation and panel behavior remain in frontend ownership
**And** product startup, path handling, and process orchestration remain in backend ownership

### Story 3.3: Surface Read-Only Semantic Inspection Beside Source Editing

As an engineer,
I want to inspect semantic information beside the authored source I am editing,
So that Athena keeps engineering meaning visible in the same workflow as text authoring.

**Acceptance Criteria:**

**Given** an Athena-authored source file is open in the editor
**When** I open the semantic inspection surface
**Then** Athena shows read-only semantic information or closely related derived inspection data beside source editing
**And** the inspection data is returned through the Athena protocol boundary from the LSP-embedded runtime

**Given** semantic inspection is visible in the workbench
**When** I review how it behaves
**Then** it does not mutate canonical semantic state directly
**And** it does not become a hidden second semantic authority in the frontend
**And** it does not pretend to be the later graphical editing layer

### Story 3.4: Keep Athena Workbench Extensions Additive Instead Of Replacing The Shell

As a platform engineer,
I want Athena-specific workbench panels and commands to attach additively to the Theia shell,
So that later milestone capabilities can be introduced without replacing the core IDE structure.

**Acceptance Criteria:**

**Given** the Athena workbench already includes repository navigation, diagnostics, and semantic inspection surfaces
**When** Athena-specific panels, commands, or menu entries are added in M4
**Then** they attach through explicit Athena-owned extension points in the product shell
**And** they do not require the main workbench structure to be rewritten to host them

**Given** the workbench contains Athena-specific additions
**When** the shell composition is reviewed
**Then** the product still reads as one coherent Theia-based IDE with one governed semantic path
**And** the workbench remains downstream of kernel and runtime authority

**Given** M4 workbench extension points are in place
**When** future milestones such as M5 repository/package features, M6 semantic SCM surfaces, or later graphical semantic-projection surfaces are considered
**Then** those future capabilities have a clear place to attach inside the same product shell
**And** M4 does not force a browser-first redesign or a replacement of the core IDE architecture
