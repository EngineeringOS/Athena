---
scope: Athena M7 graphical projection and visual workbench proof
purpose: build-substrate
altitude: initiative
updated: 2026-07-09T21:18
---

- (constraint) Inherited M4-M6 rules stay binding for M7: ide/lsp remains the only IDE semantic entry point, JVM runtime remains session authority, and graphical workbench state stays downstream of canonical semantic state.
- (decision) AD-27 binds a dedicated kernel view-model boundary so graphical delivery consumes stable projection-facing contracts instead of raw engineering entities or renderer-specific shapes.
- (decision) AD-28 binds engineering identity to the engineering object graph and keeps view definitions, notation mappings, and renderer assets downstream of canonical semantics.
- (decision) AD-29 binds layout and geometry to view-scoped projection metadata so graphical coordinates and routing do not become engineering truth.
- (decision) AD-30 binds the graphical workbench to runtime-owned projection sessions exposed through Athena-owned transport rooted at ide/lsp, with any graph framework adapter kept downstream.
- (decision) AD-31 binds M7 to inspect-first interaction so any persisted graphical mutation must route through governed runtime commands rather than frontend-local canvas state.
- (decision) AD-32 and AD-33 bind the first renderer proof to a relationship-forward object-graph projection and keep domain-specific renderer packs in extensions rather than kernel forks.
- (direction) M7 architecture keeps GLSP-class transport as an implementation-level option but refuses to let a graph framework define Athena projection contracts.
- (decision) Checkpoint party review tightened AD-27 by renaming the kernel boundary to projection-model so the spine uses one load-bearing noun consistently with the PRD projection model concept.
- (decision) Checkpoint party review tightened AD-29 and AD-31 so stale projection state is invalidated on upstream change and unapproved frontend interactions remain transient and are discarded on refresh.
- (decision) Checkpoint party review tightened AD-30 so Athena-owned transport is limited to typed projection queries, deterministic projection updates, and an explicit governed command allowlist, while graph adapters remain translation-only.
- (decision) Checkpoint party review tightened AD-32 so the first renderer proof must remain useful without notation-specific asset packs and cannot drift into notation-first semantics.
