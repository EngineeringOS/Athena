---
scope: Athena M5 repository and package graph proof
purpose: build-substrate
altitude: initiative
updated: 2026-07-08T22:18
---

- (decision) AD candidate: Source-level package imports resolve only through the Athena manifest-and-lock governed package graph rather than JVM classpath coincidence, raw filesystem traversal, or frontend-local heuristics.
- (constraint) Inherit M4 semantic boundary decisions: Theia frontend/backend stay separate from ide/lsp, ide/lsp remains the only semantic entry point, one repository maps to one active runtime-owned session per product window, and workbench state stays downstream of JVM semantic authority.
- (version) M5 inherits the active Athena stack already present in the workspace: Java 25, Kotlin 2.4.0, Gradle 9.6.1, Yarn 1.22.22, Node 22+, and Eclipse Theia 1.73.1.
- (decision) AD candidate: M5 resolver scope stays local-first and deterministic, proving repository-local state and explicit local dependency sources before registry or git transport concerns.
- (decision) AD candidate: Any M5 IDE work must stay additive and operability-scoped, exposing package graph state, diagnostics, and resolution actions without widening into broad workbench polish or a second semantic authority.
- (decision) AD candidate: M5 narrows the first repository/package contract to one repository-root athena.yaml and one repository-root athena.lock; athena.yaml carries authored intent while athena.lock is derived deterministic resolution state.
- (decision) AD candidate: M5 proves one primary package per repository and a semantic package graph composed of that root package plus resolved dependencies; package-local manifests and multi-package repository authoring are deferred.
- (decision) AD candidate: M5 introduces a dedicated kernel/repository-model boundary for repository manifest, lock, package identity, dependency declarations, and resolved package-graph report types so compiler, runtime, and IDE do not invent parallel models.
- (decision) AD candidate: The M4 repository session upgrades into a runtime-owned RepositoryGraphSession that carries manifest state, lock state, resolved package graph, and package diagnostics, while Theia remains an additive downstream consumer through ide/lsp.
- (event) spine finalized
