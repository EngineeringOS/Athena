---
scope: Athena M4 Theia platform proof
purpose: build-substrate
altitude: initiative
updated: 2026-07-08T14:08
---

- (constraint) Existing M0 to M3 kernel and runtime boundaries remain binding; the Theia product shell must stay downstream of semantic authority.
- (constraint) M4 scope is limited to the Athena Theia platform proof; repository manifest/lock/package graph is deferred to M5 and semantic SCM is deferred to M6.
- (version) Reality-check: Eclipse Theia remains an official platform for building custom desktop and cloud IDEs, with frontend/backend architecture and composition guidance, validated against theia-ide.org and official GitHub release surfaces on 2026-07-08.
- (decision) AD candidate: Athena uses a Theia-based product architecture with an ide/ module group rather than extending the existing apps/ or ui/ groups for the primary IDE path.
- (decision) AD candidate: Theia frontend, Theia backend, and Athena LSP are separate owners; frontend owns presentation, backend owns product shell and process orchestration, and the LSP process embeds Athena runtime and semantic services.
- (decision) AD candidate: M4 uses one active Engineering Repository per product window to avoid multi-root ambiguity and to align the workbench with the existing single active workspace/runtime model.
- (decision) AD candidate: Repository session authority, diagnostics, navigation, and semantic inspection flow through Athena LSP custom and standard protocol methods rather than direct frontend calls into kernel or a second custom semantic service.
- (decision) AD candidate: Theia product startup bundles a curated capability set and disables marketplace-style extension sprawl in M4; Athena owns the shipped workbench composition.
- (decision) AD candidate: Semantic inspection remains read-only in M4 unless it routes through an already existing runtime-owned command path; inspection panels never mutate semantic state directly.
- (question) Whether M4 should create a dedicated ide/protocol module or keep the first custom request contract co-located with ide/lsp remains open for implementation planning.
- (assumption) A small Athena-specific protocol contract is needed for semantic inspection and repository-session requests beyond standard LSP methods; its exact schema can remain a seed owned by implementation.
- (change) Created the final M4 architecture spine with a new ide/ product group, Theia frontend/backend split, and Athena LSP as the sole semantic entry point for the IDE path.
- (event) spine finalized
- (constraint) Implementation reference: reuse D:/Aaron/workspace/projects/2026/eos/theia-ide as the known-good official Theia IDE sample baseline because this sibling workspace has already been debugged locally, passes build, and runs on the current workstation.
- (decision) Added AD-10 to capture the GLSP-driven planning guardrail: future graphical projection under Theia must stay downstream of canonical semantic state, while M4 remains text/LSP-first and does not implement a graph server.
