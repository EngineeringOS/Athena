---
scope: M41 Spatial Reality milestone: placement, routing subsystem, geometry, quality facts, grid references, proof
purpose: build-substrate
altitude: feature
updated: 2026-08-03T10:25
---

- (constraint) Inherit M40 AD-9..AD-19 read-only: Projection authoritative owner of views; composition one capability; ProjectionConstruct domain-neutral; Spatial consumes Projection; quality metrics acceptance facts; M40 proof milestone-local.
- (decision) AD-20: Spatial is the milestone authority for geometry (placement, bounds, anchors, lanes, routes, alignment, grid references, quality).
- (decision) AD-21: placement is derived and explainable - pure function of the projection snapshot with plain-language placement reasons; no authored coordinates.
- (decision) AD-22: routing is a Spatial subsystem; route first/last points equal placed anchor points (M38 invariant); orthogonal route facts with lanes.
- (decision) AD-23: no routing optimization in M41 (lane/bend/crossing/bundle/trunk/multi-sheet) - M45.
- (decision) AD-24: quality facts are measured milestone facts computed from compiler data (no pixels), cross-referenced to the M40 baseline.
- (decision) AD-25: grid references are Spatial facts feeding the M43 Excel export.
- (decision) AD-26: geometry validation fails closed before Presentation with plain diagnostics.
- (event) M41 architecture spine distilled from M41 PRD and M40 spine.
- (constraint) Replacement PRD FR-1 through FR-13, NFR-1 through NFR-5, SM-1 through SM-4, and GATE-1 through GATE-7 bind this spine.
- (constraint) Code reality-check found row placement, bridge-owned pseudo-envelopes/global grid, fallback mapNotNull routing, and multiple Spatial orchestration callers; recovery must remove these competing paths.
- (decision) AD-20 amended: SpatialDocument contains complete typed per-Sheet facts with stable identities, ownership, and source trace; no raw maps or overloaded IDs.
- (decision) AD-21 amended: ProjectionSpatialLayout derives canonical 2D placement under exact sheet, gutter, spacing, padding, and explicit unassigned-region policy.
- (decision) AD-22 amended: exact per-port anchors feed obstacle-aware orthogonal routing; every connection routes once or complete Spatial compilation fails.
- (decision) AD-24 amended: quality metrics use PRD FR-12 formulas per Sheet from validated facts; M41 has no label metric.
- (decision) AD-25 amended: typed Grid References derive from occurrence/envelope centers using the owning Sheet grid only.
- (decision) AD-26 amended: one complete validator accumulates deterministic actionable diagnostics and blocks Presentation on any issue.
- (decision) AD-27: ProjectionSpatialCompiler is the only Projection-to-Spatial orchestration entry; callers migrate and stale bridges/transformations are deleted.
- (decision) AD-28: current layout and routing engines remain internal deterministic geometry solvers behind domain-neutral adapters; solver and electrical types do not enter Spatial contracts or Athena syntax.
- (decision) AD-29: Presentation mapping preserves Spatial coordinates and route order immutably; E2E proof reads runtime payloads and pixels, never hardcoded proof flags.
- (decision) AD-30: compiler normalization owns canonical ordering and stable fact/source-trace identity across repeated and permuted input.
- (event) M41 architecture spine finalized after lint, reality-check, and adversarial divergence review; zero unresolved findings.
