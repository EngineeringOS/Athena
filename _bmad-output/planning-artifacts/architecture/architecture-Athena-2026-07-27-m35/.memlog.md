---
scope: M35 Physical Installation Model and Cabinet Composition
purpose: Build substrate for BMAD epics, stories, and implementation agents
altitude: feature
updated: 2026-07-27T12:34
---

- (decision) M35 uses a compiler-owned physical projection pipeline; this binds all epics to authored engineering and installation intent -> PhysicalInstallationIR -> Cabinet composition -> Graphic Primitive IR and prevents renderer-owned physical truth.
- (constraint) M34 authority remains binding: Athena source owns metadata, BindingResolver selects, RepresentationBindingCompiler constructs, GraphicPrimitive is the active visual vocabulary, and the renderer paints only compiled payloads.
- (decision) Physical installation intent is authored source truth while PhysicalInstallationIR v0 is deterministic derived output; neither Presentation IR nor renderer state may become physical or semantic authority.
- (decision) kernel:physical-model owns physical installation contracts, ids, units, topology, diagnostics, and pure constraint evaluation; it may depend on engineering-model but never representation, presentation, package-runtime, LSP, or Theia.
- (decision) M35 extends existing PhysicalTraitDefinition and ResolvedPhysicalTraitDefinition as read-only installation inputs and introduces no Engineering Component System, product catalog language, Physical Installation Reference Package, or new package kind.
- (decision) PhysicalInstallationIR v0 uses stable ids and source provenance for InstallationSpace, Enclosure, MountingSurface, Rail, Duct, MountedOccurrence, TerminalGroup, RouteChannel, ClearanceZone, and routed connection facts; physical measurements use integer millimetres and container-local physical coordinates, never pixels or drawing-grid coordinates.
- (decision) Every MountedOccurrence has exactly one canonical semantic subject, one physical container or mounting surface, one orientation admitted by its Physical Installation Contract, and one stable occurrence id; container cycles, duplicate ids, orphan mounts, and incompatible mount types fail closed.
- (decision) Physical Constraint Evaluation v0 is deterministic validation only: containment/fit, discrete-orientation footprint, collision, clearance, and container compatibility; it performs no optimization, automatic placement, general solving, or AI layout.
- (decision) M35 extends existing athena.yaml repository/package descriptor intent and compiler-generated athena.lock resolution evidence; no package.athena, second manifest, runtime descriptor interpretation, or namespace-as-product-identity is allowed.
- (decision) Package resource admission is two-phase and AST-driven: securely snapshot Athena source, compile typed resource declarations, capture only declared package-local resources with no-follow/root checks, then freeze one immutable admitted snapshot; regex source scanning is retired.
- (decision) PackageResourceKey is logical package id/version + owning source unit + declared resource id; snapshot/cache identity additionally includes normalized path, content hash, compiler/schema version, and athena.lock digest. Compile, render, and E2E perform no network resolution.
- (decision) SVG is untrusted geometry input only. Athena owns anchors, labels, roles, directions, signals, identity, lifecycle, profiles, and binding. SVG may expose id or optional data-athena-geometry-ref hints; missing referenced geometry fails representation admission, while hint removal cannot change the semantic model.
- (decision) Physical installation compilation and representation selection remain parallel authorities. CabinetCompositionCompiler joins MountedOccurrence and resolved RepresentationOccurrence by stable semantic/occurrence identity; visual bounds never determine footprint and physical constraints never select an Element.
- (decision) RouteChannel topology belongs to PhysicalInstallationIR; semantic connections own endpoints; routing derives channel-constrained physical route facts bound to explicit representation terminal anchors. Missing anchors, off-channel segments, body intersections, or unbound endpoints block success.
- (decision) Every composed Cabinet occurrence carries a typed GraphicOccurrenceTrace to semantic subject, installation intent, binding rule, representation definition/resource snapshot, and source spans. Selection resolves through the interaction subject index, never DOM/SVG inspection.
- (decision) M35 proves trace and the future governed edit contract only. Any later move/replace edit must use ActionIntent -> AuthoringTransaction -> SourceMutationPreview -> compile/lint -> accept/reject -> rerender; direct mutation of SVG, GraphicPrimitive, Presentation IR, or renderer state is forbidden.
- (decision) Public language additions are limited to minimum domain-facing installation intent and typed resource declarations. ANTLR4 remains semantic parser authority; Tree-sitter provides editor parsing/highlighting only and must share a corpus contract. Package/path/resource validation consumes AST, not regex.
- (decision) Cabinet is M35's only visible product surface. Completion requires one dedicated example, zero diagnostics/fallback/XML/raw-markup authority, structured physical/routing/trace proof, fresh desktop and narrow Electron screenshots, and nonblank canvas evidence; screenshots are structural review, not pixel authority.
- (decision) M35 remains a local offline Windows/Electron product slice with no new service, database, registry, deployment, or infrastructure. Runtime and renderer consume immutable compiler outputs only.
- (decision) Every M35 story ends with polish/purge and removes touched legacy regex scanners, unreleased XML paths, deprecated SVG metadata authority, stale fixtures, generated evidence, and duplicate compatibility code unless a current test proves necessity.
- (version) Brownfield stack verified from repository: Kotlin 2.4.0, Gradle 9.6.1, ANTLR 4.13.2, LSP4J 0.23.1, Theia 1.73.1, TypeScript 5.9.x, Node >=22, tree-sitter-cli >=0.26.1, web-tree-sitter 0.26.x.
- (decision) PhysicalInstallationContractResolver in physical-model resolves each field independently: explicit project value overrides resolved trait value, same-precedence duplicates fail ambiguous, missing required fields fail before IR, each resolved field carries provenance, and the canonical resolved record owns the digest.
- (decision) InstallationOccurrenceKey is the sole Cabinet physical/representation join key, derived from resolved repository coordinate, normalized source unit, installation declaration id, and canonical semantic subject; Cabinet v0 requires exactly one physical and one representation occurrence per key.
- (decision) Physical topology uses sealed relations: Enclosure in InstallationSpace; MountingSurface/Duct/TerminalGroup/RouteChannel/ClearanceZone in Enclosure; Rail mounted on MountingSurface; MountedOccurrence has one Enclosure container and one MountingSurface/Rail/TerminalGroup mount target; Duct and RouteChannel are never mount targets.
- (decision) PhysicalFrame2D v0 uses upper-left origin, +X right, +Y down, clockwise 0/90/180/270 orientations, post-rotation AABB upper-left placement, center rotation, and child-local then parent-local transforms. One shared transform implementation serves constraints, anchors, routing, composition, and proof.
- (decision) CabinetVisualTransform v0 maps intrinsic representation bounds uniformly with aspect ratio preserved and centered into the post-orientation physical footprint, then applies physical-to-drawing transform; one transform id applies to body, anchors, labels, hotspots, bounds, routes, and trace.
- (decision) ResolvedPackageCoordinate comes only from athena.yaml graph; AthenaNamespace comes only from source; SourceUnitId is coordinate plus normalized package-root-relative path; PackageResourceKey is SourceUnitId plus resource declaration id. Paths are slash-normalized NFC and case/normalization collisions fail.
- (decision) AdmittedPackageSnapshotDigest excludes generated lock bytes and hashes normalized descriptor intent, resolved coordinates, sorted staged source/resource hashes, and compiler/schema version. athena.lock records that digest; locked mode validates an existing lock as input evidence without creating a digest cycle.
- (decision) ResolvedRepositoryPackageSnapshot is the one aggregate package admission handle, ordered by ResolvedPackageCoordinate, with one AdmittedPackageSnapshot per coordinate; overlapping roots or one physical file admitted twice fail, and budgets apply per package and repository aggregate.
- (decision) SvgGeometryNodeIndex uses XML id as the only lookup key; optional data-athena-geometry-ref must equal id. IDs are unique after safe use expansion and lookup returns normalized node plus accumulated transform. M34 parser hardening, closed subset, normalization, and per-file/package budgets remain binding.
- (decision) PhysicalRect v0 is closed axis-aligned post-orientation geometry. Positive-area overlap is collision; edge contact is allowed. Four-sided clearance inflates its owner; either inflated rect intersecting the other footprint violates clearance; inflated rect must fit container; compatibility uses typed-id sets.
- (decision) PhysicalInstallationIR owns RouteChannelTopology, obstacles, and PhysicalRouteIntent only. drawing-composition owns CabinetRoutingCompiler and CabinetRouteFact using bound anchors and CabinetVisualTransform. Existing schematic routing-model is not the M35 physical route authority.
- (decision) presentation-model owns versioned GraphicOccurrenceTraceTable keyed by GraphicOccurrenceId; selectable primitives carry occurrence id only. Subjects are mounted semantic, installation declaration, or engineering connection without synthetic semantic ids; decorative primitives are nonselectable and excluded by explicit proof.
- (decision) Future MoveMountedOccurrence targets installation source, ChangeRepresentationBinding targets binding/profile source, and engineering replacement targets engineering source; generic graphic or representation move is forbidden and all actions use one M31 AuthoringTransaction.
- (decision) M35 Cabinet does not consume existing schematic LayoutIntent/SchematicPlacementFact or legacy GeometryDocument paths. Existing Control Drawing retains regression tests; old Cabinet-specific Geometry/Presentation producers gain no new callers and are migrated/deleted after zero-caller proof.
- (decision) PhysicalInstallationIR owns physical extents; Cabinet composition derives drawing bounds from enclosure, mapped occurrences, rails, ducts, terminal groups, routes, labels, frame, and padding; PresentationDocument owns viewport from those bounds; renderer never hardcodes bounds.
- (decision) M35 structural visual gate requires visible enclosure, mounting surface/rail, duct/route channels, terminal group, mounted controls, readable labels, contained routes, professional density, no clipping/overflow/unintended overlap, and complete trace at 1920x1080 and 1280x900 plus narrow viewport.
- (decision) athena.yaml owns package coordinate/dependency/source-root intent only; Athena source declarations own resources and exported definitions; compiler derives export/resource evidence; athena.lock/proof record admitted hashes. Resource syntax is resource <id> { kind svg path './asset.svg' } and graphic references that id.
- (version) M35 pins TypeScript 5.9.3, tree-sitter CLI 0.26.1, and web-tree-sitter 0.26.0 in workspace manifests/lock as part of language/editor synchronization.
- (decision) M35 explicitly supersedes M34 AD-3/4/5 metadata-authority and AD-8 annotated-SVG importer output, retires data-athena-schema and semantic data-athena attributes, and retains M34 AD-12/13 safe geometry parsing, limits, expansion, and normalization.
- (decision) Resource paths resolve only from the declaring SourceUnit directory inside its admitted package snapshot; direct cross-package physical paths are forbidden and dependencies are reached through exported logical keys.
- (decision) Athena namespace/path mapping is exact lowercase source-root-relative parent equality; default package, case-only alternatives, and Unicode normalization collisions fail admission.
- (decision) PhysicalInstallationContractV0 is closed: positive width/height/depth, mounting type, non-empty allowed orientations, four non-negative clearance sides, and non-empty compatible container kinds; scalar fields override independently, sets replace atomically, and canonical field order owns the digest.
- (decision) Existing unchecked PhysicalSize remains legacy trait input only; PhysicalInstallationContractResolver validates and converts it into M35 validated measurements before resolved contracts or PhysicalInstallationIR.
- (decision) Enclosure is the sole v0 physical container; mounted occurrences target only MountingSurface, Rail, or TerminalGroup; RouteChannel belongs to exactly one Duct and no ClearanceZone node exists in v0.
- (decision) Authored placement is mount-target-local; surface and terminal frames are upper-left, rail X follows direction and rail Y follows declared normal.
- (decision) CabinetVisualTransform has one normative seven-stage matrix order and golden body/anchor tests at 0, 90, 180, and 270 degrees.
- (decision) Resource ids are lexical to one Athena source unit; dependencies export compiled Symbol/Element definitions and never raw resources.
- (decision) Repository-model owns RepositoryManifest package roots and replacement RepositoryLockV2 schema/codec; compiler solely validates and atomically materializes lock state; no v1 compatibility path remains.
- (decision) ValidatedLockStateDigest hashes canonical lock facts, uses lock-state/v1:unlocked before validation, never enters snapshot identity, and validate/update modes fail closed with stable diagnostics.
- (decision) PackageAdmissionLimitsV1 in package-model fixes non-overridable file, package, repository, SVG expansion, path, primitive, and work-unit budgets shared by admission, proof, diagnostics, and tests.
- (decision) SVG ids remain lookup keys; a referenced id must materialize exactly once after safe use expansion while unreferenced geometry may repeat.
- (decision) CabinetRoutingCompiler v0 uses authored ordered channels and one fixed no-search lane/stub/bend algorithm; invalid adjacency, capacity, containment, compatibility, or intersection fails.
- (decision) Trace selection reuses InteractionSubjectResolver and SemanticCapabilityRegistry; M31 editing reuses exact SemanticActionIntent, SemanticAuthoringTransaction, AuthoringPreview, and AuthoringSourceEditEvidence contracts; frontend prefix inference is deleted.
- (decision) The architecture now includes a complete touched-module dependency graph and an EXISTING/EXTEND/REPLACE/NEW IN M35 ledger so adopted decisions are not mistaken for implemented APIs.
- (decision) The normative M35 DSL surface is fixed in the PRD addendum and ANTLR4/Tree-sitter parity extends examples/m23/parser-parity-proof.
- (decision) RepositoryManifest owns authored package coordinate intent only; new ResolvedPackageCoordinate is derived graph/snapshot and RepositoryLockV2 state.
- (decision) The exact future edit chain includes governed capability/command translation from SemanticActionIntent to existing AuthoringIntent before SemanticAuthoringTransaction.
- (decision) The dependency diagram is explicitly a load-bearing M35 subgraph; unchanged direct Gradle dependencies remain code-owned, while every M35-required edge remains explicit in diagram or ledger.
- (decision) Rail v0 frame polarity is fixed from orientation: horizontal local axes map to enclosure +X/+Y; vertical local +X maps enclosure +Y and local +Y maps enclosure +X.
- (decision) RouteChannel source declares a duct-interior-local rectangle plus orientation, lanes, and margin; no geometry is inferred from a duct.
- (decision) Routed engineering connections require source-unit-unique authored aliases; EngineeringConnectionId is SourceUnitId plus alias, and group names remain organizational only.
- (decision) V0 usable enclosure depth equals authored depth, in-plane rotations do not change depth, and occurrence depth must fit.
- (decision) Physical constraint evaluation requires selected orientation membership and target-specific mounting/fit rules for surfaces, terminal groups, and rails.
- (decision) Terminals are ordinary MountedOccurrences targeting TerminalGroup; the group owns deterministic ordering by along-axis position, cross-axis position, then occurrence key.
- (decision) Vertical Rail uses right-handed rigid basis local +X=enclosure +Y and local +Y=enclosure -X; all target transforms have determinant +1 and vertical body/anchor tests prove no reflection.
- (decision) Physical validation requires MountingSurface, Duct, and TerminalGroup bounds inside Enclosure and each complete oriented Rail interval inside its MountingSurface.
- (decision) Route lane centres use exact rational formula M + ((2i+1)*(S-2M))/(2N), with positive usable span, stable-id capacity allocation, no pre-drawing rounding, and odd/full-capacity golden tests.
- (decision) RouteChannelTopology derives adjacency only for same-duct disjoint rectangles sharing one positive trimmed boundary segment; cross-duct, corner, overlap, gap, and multiple intersections fail.
- (decision) Installation cabinet is a system member, cabinet is the only M35 installation kind, and route is an installation member resolving only same-source-unit connection aliases.
- (decision) Every grouped and ungrouped connection requires an alias in M35; alias-free grammar, AST, lowering, examples, and fixtures are migrated and deleted with no compatibility parser.
- (event) spine finalized after zero-blocker rubric, brownfield, and divergence reviewer gates
