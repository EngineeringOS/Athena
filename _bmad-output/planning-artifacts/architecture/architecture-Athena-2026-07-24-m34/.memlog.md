---
scope: M34 Symbol and Element representation authority
purpose: Correct M33 XML runtime authority and produce a secure Cabinet representation path
altitude: milestone-to-epics
updated: 2026-07-24T16:10
---

- (decision) AD-1..AD-8 recovered from the existing M34 spine and retained with stable IDs.
- (decision) Symbol owns atomic visual anatomy only; it never owns project semantic ports.
- (decision) Element owns reusable visual composition and anchor compatibility constraints; it never reclassifies a device.
- (decision) One definition has one metadata authority: Athena source. SVG may contribute geometry only.
- (decision) Existing RepresentationDescriptor, PresentationProfile, BindingResolver, and Graphic Primitive IR contracts are reused or extended; no parallel fourth IR.
- (decision) SVG is hostile compiler input and must pass a closed allowlist, resource limits, reference validation, and root-coordinate normalization before IR.
- (decision) Intrinsic asset viewBox may be fixed; Cabinet document viewBox is derived from compiled composition bounds.
- (decision) M34 execution is vertical: canonical contract proof, secure portable authoring proof, governed Cabinet migration, professional Cabinet product proof.
- (decision by user) Athena *.athena language source is the sole metadata frontend; SVG is geometry-only input and annotated SVG must be converted to canonical Athena source before admission.
- (decision) RepresentationDefinition is canonical; RepresentationDescriptor is generated only, DrawingSymbolAnatomy is migration-only, and no new Element IR exists.
- (decision) BindingResolver is sole selector with exact snapshot versions, explicit priority, ambiguity failure, and deterministic variant rules; RepresentationBindingCompiler only builds occurrences.
- (decision) GraphicPrimitive is the only active Cabinet visual vocabulary; PresentationPrimitive and direct markup/box paths have named migration callers and deletion gates.
- (decision) Element V1 composes Symbols and local geometry only with explicit child id, transform, z-order, and anchor export; kernel/drawing-composition remains project sheet/layout authority.
- (decision) Safe SVG uses fail-closed parser setup, exhaustive attribute/value grammars, no-I/O resolution, per-file and aggregate budgets, immutable staged acquisition, markup-incapable transports, and instrumented E2E.
- (decision) Element owns visual composition only; reusable engineering product knowledge and multi-view representation sets belong to a future Engineering Component System.
- (decision) The public M34 grammar has a vocabulary budget; implementation types remain compiler/runtime contracts.
- (superseding decision by user) Native Athena is the simple representation frontend; complex
  representations may be one governed annotated SVG source with versioned closed `data-athena-*`.
- (superseding decision) One definition still has one authority, now selected exclusively between
  `NATIVE_ATHENA` and `GOVERNED_SVG`. Cross-format duplicate identity and sidecar field merging fail.
- (decision) Source-specific ASTs share one compiler-owned canonical admission/validation path and
  lower to `RepresentationDefinition` plus `GraphicPrimitive`; no SVG runtime IR, DOM transport, XSD
  authority, XML manifest compatibility, or frontend metadata interpretation exists.
