---
scope: Athena M6 semantic SCM proof
purpose: build-substrate
altitude: initiative
updated: 2026-07-09T15:35
---

- (constraint) Inherit M5 repository/package authority decisions: repository-model remains canonical and vendor-neutral, athena.lock remains derived state, RepositoryGraphSession remains the active runtime-owned repository session, and IDE work remains additive through ide/lsp.
- (constraint) Inherit M4 IDE boundary decisions: ide/lsp remains the sole semantic entry point, one repository maps to one active session per product window, session authority remains in the LSP-embedded JVM runtime, and workbench state stays downstream of semantic authority.
- (constraint) Inherit M3 hosted-extension discipline: stable plugin contracts remain separate from compiler internals, and plugin-owned semantic enrichment must stay additive rather than sovereign.
- (version) M6 inherits the active Athena stack already present in the workspace: Java 25, Kotlin 2.4.0, Gradle 9.6.1, Yarn 1.22.22, Node 22+, and Eclipse Theia 1.73.1.
- (decision) AD candidate: M6 introduces a dedicated VCS-neutral semantic SCM core above repository-model and RepositoryGraphSession rather than extending repository-model with vendor or UI vocabulary.
- (decision) AD candidate: semantic baselines are repository-scoped comparison inputs and must not be reconstructed solely from transient command history or raw file lists.
- (decision) AD candidate: semantic diff compares canonical repository/package and engineering states through the same JVM semantic pipeline; raw file diff is only supporting evidence.
- (decision) AD candidate: vendor adapters such as Git are substrate-only and should live in a separate integrations layer rather than kernel or ide.
- (decision) AD candidate: Theia SCM is a downstream bridge for workbench contribution shape only and must not define Athena semantic SCM contracts.
- (decision) AD candidate: semantic review and commit preparation outputs are typed inspectable artifacts that separate authored-intent change from derived consequences such as lock churn or validation fallout.
- (decision) AD candidate: domain-specific review enrichment is allowed only through governed hosted-plugin contracts and may not suppress generic semantic SCM facts.
- (decision) AD candidate: publish-oriented semantic history in M6 stays package-identity anchored and transport-light, deferring registry and ecosystem distribution flows.
- (event) spine finalized
