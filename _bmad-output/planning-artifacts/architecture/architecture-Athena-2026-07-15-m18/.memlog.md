---
scope: Athena M18 Project Semantic Graph And Package Resolution
purpose: Fast-path architecture spine for implementation planning
altitude: feature
updated: 2026-07-15T15:38
---

- (constraint) Input PRD binds M18 to project semantic graph and package resolution, not a broad language redesign, import-only syntax milestone, registry milestone, renderer milestone, or frontend semantic-resolution milestone.
- (constraint) Existing repository reality includes AthenaRepositoryGraphResolver in kernel/compiler for deterministic local package graph resolution from governed repository contract state.
- (constraint) Existing language reality keeps ANTLR parse authority in kernel/language and exposes compiler parse/lower paths through AthenaCompiler; Engineering IR remains canonical semantic output.
- (constraint) Existing LSP reality exposes definition and references through AthenaLanguageFeatures navigation indexes and AthenaLanguageServer LSP methods; M18 must extend that path rather than create frontend navigation authority.
- (decision) Adopt compiler-owned semantic-workspace pipeline as the M18 paradigm: repository graph plus authored AST feed a project semantic graph before lowering.
- (decision) Bind package and import declarations to ANTLR compiler parsing and Athena-owned authored AST contracts; generated parser types must not leak as milestone contracts.
- (decision) Resolve imports only through governed repository graph state and source-unit availability; raw filesystem traversal, JVM classpath coincidence, and frontend heuristics are invalid import authorities.
- (decision) Introduce Project Semantic Graph as a compiler-owned model that joins resolved packages, source units, exported/available declarations for the proof slice, symbol bindings, diagnostics, and provenance.
- (decision) Use declaration-level symbol linking as the narrow M18 proof slice because it demonstrates real cross-unit binding without forcing a broad declaration or visibility system.
- (decision) Package-aware diagnostics are compiler diagnostics projected through LSP; missing package, missing symbol, invalid availability, and cycle/graph-invalid cases must carry stable codes and provenance.
- (decision) Lowering consumes linked authored meaning from the project semantic graph; imports must not be implemented as AST paste, source include, or hidden expansion.
- (decision) LSP package-aware behavior is a projection of compiler semantic workspace snapshots; go-to-definition, references, diagnostics, and workspace/document symbols may read indexes derived from compiler-owned bindings only.
- (decision) Tree-sitter mirrors package/import syntax for syntax UX only and may never produce package resolution, symbol linking, or semantic diagnostics.
- (decision) Semantic namespace identity must preserve package id, source unit id, declaration id, source span, and imported capability provenance strongly enough for diagnostics/navigation and later governed consumers.
- (decision) Proof corpus under examples/m18 must include single-package, cross-package success, invalid import, unresolved symbol, graph-invalid/cycle, and vendor/governed package examples wired to executable tests.
- (assumption) Fast-path assumption: package declaration plus package import and symbol-target import is the first supported syntax slice; aliasing is deferred unless implementation needs it to disambiguate test fixtures.
- (assumption) Fast-path assumption: no full export/visibility system lands in M18; availability for the proof slice is package-graph admitted declarations plus explicit imports.
- (question) Confirm whether workspace symbol support is required for M18 closeout or can remain secondary after diagnostics, definition, and references.
- (question) Confirm whether the semantic namespace proof must include one downstream governed capability such as component knowledge in the first implementation slice.
- (event) Fast-path architecture drafting selected by Aaron; spine-only deliverable assumed.
- (event) memlog seeded for M18 architecture spine drafting
- (decision) Reviewer gate fix: Project Semantic Graph snapshot contract now binds graphId, packages, sourceUnits, namespaces, declarations, bindings, diagnostics, and shared derivation for LSP/lowering/tests.
- (decision) Reviewer gate fix: canonical identity algorithms now bind package keys, source unit ids, declaration ids, namespace ids, binding ids, and graph ids.
- (decision) Reviewer gate fix: LSP package-aware behavior must project a compiler-owned semantic graph snapshot and internally preserve snapshot graphId.
- (decision) Reviewer gate fix: M18 semantic namespace proof must preserve at least one governed capability provenance marker such as component-knowledge availability.
- (event) Applied rubric and adversarial reviewer gate fixes to architecture spine.
- (event) reviewer gate fixes logged
- (event) spine finalized
