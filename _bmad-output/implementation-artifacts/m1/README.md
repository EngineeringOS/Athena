# M1 Implementation Artifacts

This folder preserves the completed M1 implementation artifacts under the milestone-standard `m1/` path.

- M1 story files, retrospectives, and sprint tracking are grouped here.
- M1 path references should use `_bmad-output/implementation-artifacts/m1/`.
- Future milestones must not place story files at the root of `implementation-artifacts/`.
