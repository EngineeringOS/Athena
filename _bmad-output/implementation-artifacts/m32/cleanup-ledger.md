# M32 Cleanup Ledger

This ledger records stale or deferred artifacts found during M32 stories. Every retained item must
have an owner, reason, target milestone, and verification path. Stories should remove stale
artifacts directly whenever practical.

## Open At Planning Start

| ID | Item | Owner | Reason | Target | Verification |
| --- | --- | --- | --- | --- | --- |
| M32-CL-001 | `AthenaAuthoringSessionRuntimeService` preview-session compatibility | M32 Story 6.1 implementation agent | M31 product authoring uses transaction evidence, but older callers still use preview/session compatibility. | M32 Story 6.1 | CodeGraph caller review plus `:kernel:runtime:test`. |
| M32-CL-002 | Non-Theia CLI/desktop/Compose relationship mutation surfaces | M32 Story 6.2 implementation agent | Theia flow is governed, but compatibility surfaces may still use low-level relationship commands. | M32 Story 6.2 | Fixed-string scan, runtime command tests, and migrated/retired surface proof. |
| M32-CL-003 | Broad Graphical View `port:` candidate affordance | M32 Story 6.3 implementation agent | Pre-preview highlighting must come from registry compatibility evidence, not semantic id prefix. | M32 Story 6.3 | Frontend test proving no `semanticId.startsWith('port:')` candidate gate remains. |
| M32-CL-004 | Legacy M26 display-title sheet-role fallback | M32 Story 6.4 implementation agent | Older payload compatibility may still depend on display title parsing. | M32 Story 6.4 | Typed policy tests plus scan proving M31/M32 samples do not depend on fallback. |
| M32-CL-005 | `_reference` occurrence legacy fixtures | M32 Story 6.4 implementation agent | Defensive legacy tests may remain after normal payloads stopped emitting duplicate visual references. | M32 Story 6.4 | No-duplicate projection tests plus scan proving retained hits are legacy tests/history only. |

## Story Entries

Story implementation agents append entries here when a stale artifact is retained beyond its story
boundary.

| ID | Item | Owner | Reason | Target | Verification |
| --- | --- | --- | --- | --- | --- |
| M32-CL-001-CLOSED | `AthenaAuthoringSessionRuntimeService` preview/session compatibility | M32 Story 6.1 implementation agent | Retained as explicit `legacy-preview-readonly-v1` compatibility API with no mutable source authority; governed acceptance still requires authorities. | Closed in M32 Story 6.1 | CodeGraph service/caller review plus `:kernel:runtime:test` and full `check`. |
| M32-CL-002-CLOSED | `AthenaConnectPortsCommand` non-Theia relationship mutation surfaces | M32 Story 6.2 implementation agent | Retained as explicit `legacy-connect-ports-runtime-command-v1` runtime compatibility command for CLI, desktop Compose, and domain-electrical runtime; product authoring remains `SemanticRelationshipIntent` and the retained command has no mutable source-authoring authority. | Closed in M32 Story 6.2 | CodeGraph exploration, fixed-string surface audit, RED/GREEN `AthenaCommandRuntimeTest`, focused `:kernel:runtime:test`, and full `check`. |
| M32-CL-003-CLOSED | Broad Graphical View `port:` relationship candidate affordance | M32 Story 6.3 implementation agent | Removed from candidate authority; Graphical View now uses `relationshipCandidateEvidence()` from semantic inspection port facts, signal compatibility, and direction compatibility before preview. Remaining `startsWith('port:')` source hit is create-entity reveal preference, not relationship candidate selection. | Closed in M32 Story 6.3 | CodeGraph frontend review, RED/GREEN `athena-m31-governed-relationship-preview.test.mjs`, fixed-string scans, `yarn test`, and full `check`. |
| M32-CL-004-CLOSED | Legacy M26 display-title sheet-role fallback | M32 Story 6.4 implementation agent | Removed from graph-glsp adapter authority; sheet role now comes only from typed `sheet.role` or normalized `policyEvidence.sheetViewRole`, while `displayName` remains visible text only. | Closed in M32 Story 6.4 | RED/GREEN graph-glsp adapter test `does not derive sheet role from displayName when typed policy evidence is absent`, `yarn test`, fixed-string scan for `resolveSheetViewRole`, and full `check`. |
| M32-CL-005-CLOSED | `_reference` occurrence legacy fixtures | M32 Story 6.4 implementation agent | Retained only as legacy defensive tests and historical fixtures for cross-reference handling; normal compiler/runtime/LSP projection payloads prove duplicate off-sheet visual reference components are not emitted. | Closed in M32 Story 6.4 | Fixed-string `_reference` scan plus focused compiler/runtime/LSP no-duplicate projection tests, M32 package smoke/density proof, and full `check`. |
| M32-CL-006-CLOSED | M32 sample had package-runtime proof but no IDE-open repository-contract smoke | M32 post-closeout implementation agent | Real Electron/LSP startup exposed missing `athena.yaml`/`athena.lock`, invalid `project` root syntax, and unsupported source-layer device types that isolated package-runtime proof did not catch. | Closed in M32 post-closeout | RED/GREEN `AthenaM32SampleProjectCompilerTest`, added M32 repository manifest/lock, repaired source syntax/types, and `yarn start:smoke:m32` proves zero diagnostics, Outline nested port, Graph Workbench, screenshot, and no fallback representation. |
| M32-CL-007 | Graph View mixes projection modes, sheet navigation, authoring actions, and package proof into one unstable toolbar | M32 Epic 7 implementation agents | Customer review exposed confusing `cabinet`/`documentation`/`schematic` peer labels, blinking sheet selector, unclear create button, overlapping create panel, and package assets that are not yet live renderer authority. | M32 Stories 7.1-7.5 | Sequential story proof: taxonomy contract, stable sheet navigation E2E, create-panel geometry E2E, package-backed live Graph View authority, final product polish/purge. |
| M32-CL-007-7.1 | Raw projection ids retained in Graph View transport and diagnostics | M32 Story 7.1 implementation agent | Story 7.1 intentionally changed product-facing labels only; ids such as `cabinet`, `documentation`, and `schematic` remain protocol ids until later stories decide visibility. | M32 Stories 7.2-7.5 | `graph-view-taxonomy-contract.md`, `athena-m32-graph-view-taxonomy.test.mjs`, focused frontend tests, `yarn build`, and encoding audit. |
| M32-CL-007-7.2 | M31-only sheet selector role assumption | M32 Story 7.2 implementation agent | Removed from selector visibility authority; retained compatibility fallback now accepts any governed multi-sheet selector with unique sheet ids and hides explicit single-sheet selectors. | Closed in M32 Story 7.2 | RED/GREEN `athena-graph-workbench-model.test.mjs`, M32 Electron smoke selector persistence proof across `cabinet`, `wiring`, and `schematic`, frontend/product builds. |
| M32-CL-007-7.3 | Create entity panel DOM-existence proof without geometry proof | M32 Story 7.3 implementation agent | Removed as an acceptance shortcut; smoke now fails collapsed/covered panels and records viewport/frontmost/control reachability evidence. | Closed in M32 Story 7.3 | RED `panelHeight=22` smoke failure, GREEN M32 smoke with `panelHeight=220`, `frontmostAtCenter=true`, `withinViewport=true`, and source CSS regression test. |
| M32-CL-007-7.5 | `cabinet`, `wiring`, and `schematic` projection buttons in the M32 customer toolbar | M32 Story 7.5 implementation agent | Retained only as protocol/programmatic compatibility; the customer-demo toolbar exposes the professional `documentation` sheet view as the single visible projection surface until another mode has product-quality representation. | Closed in M32 Story 7.5 | `athena-m32-graph-view-taxonomy.test.mjs`, M32 product smoke `projectionViewProof`, rebuilt Theia product bundle, and `yarn start:smoke:m32`. |
| M32-CL-007-7.6-CLOSED | Story 7.5 Documentation-first product policy and graph-first source-editor prerequisite | M32 Story 7.6 implementation agent | Live customer review reversed the 7.5 product decision: Cabinet is the primary visible Graph View, Documentation remains programmatic compatibility with contextual navigation, and Create Device is backend-authorized without opening an editor. The unopened-source path now rejects stale disk revisions and avoids `workspace/applyEdit` deadlock. | Closed in M32 Story 7.6 | Frontend `202/202`, graph-glsp `7/7`, LSP authoring `18/18`, root Gradle `check` (147 tasks), rebuilt Theia product, and M32 Electron proof of acceptance, nested source persistence, projection, and reopen persistence. |
