# M30 Final Purge And Regression Report

Date: 2026-07-21
Story: 7.3

## Stale Artifact Audit

The final stale artifact audit covered stale docs, old screenshots, obsolete renderer fallbacks,
dead sample paths, and misleading design claims.

Findings:

- Old screenshots: `_bmad-output/implementation-artifacts/m30/screenshots/m30-graph-workbench-smoke.png` is retained as the Story 6.3 visual proof artifact and ledgered in `cleanup-ledger.md` at 51038 bytes. The M27 proof screenshot was regenerated by the M27 regression smoke and remains owned by the M27 proof path.
- Obsolete renderer fallbacks: M27/M28/M29/M30 product proofs reported no fallback representation ids in accepted smoke payloads.
- Dead sample paths: M27, M28, M29, and M30 sample project smoke scripts resolved their checked-in sample project manifests and source files.
- Misleading design claims: the M30 executable smoke assertion and Story 5.4 still expected the old Documentation default. Both were updated to the current Cabinet default and ledgered as `M30-CL-004`.
- Smoke-race cleanup: the M30 Outline proof could snapshot `paths: []` before the Outline model populated. The frontend smoke hook now waits for non-empty Outline paths before proving nested-port structure.
- QET runtime dependency claims: M30 docs and sample README keep QET as reference/import design only; no runtime QET path was introduced.
- `.tools`: final status audit found no staged or untracked `.tools` path.

## Regression Evidence

The product smokes were run sequentially after the final smoke-harness hardening:

- M27 regression smoke: `yarn --cwd ide start:smoke:m27` passed.
- M28 regression smoke: `yarn --cwd ide start:smoke:m28` passed after the shared Electron harness bounded the Graphical View reveal command and sheet-switch API so older proof paths cannot hang until the outer kill timeout.
- M29 regression smoke: `yarn --cwd ide start:smoke:m29` passed and proved the Cabinet default plus nested Outline port path.
- M30 product smoke: `yarn --cwd ide start:smoke:m30` passed, captured the M30 screenshot, reported 4 representation occurrences, 9 routes, 9 terminal-anchored routes, zero center fallback route ids, zero route/body intersections, transparent normal chrome, and the expected nested Outline port path.

## Duplicate Sprint-Key Audit

The final Node guard includes a duplicate development status key check for
`_bmad-output/implementation-artifacts/m30/sprint-status.yaml`.

Result: no duplicate development status keys.

## Encoding

Encoding audit passed in final verification with:

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\encoding-audit.ps1
```

## Closeout

No stale M30 artifact was left unowned. Retained or historical artifacts are recorded in
`cleanup-ledger.md`; executable product assertions now match the accepted M29/M30 Graphical View
default behavior.
