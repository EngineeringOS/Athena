# Epic 3 Retrospective

Milestone: `M8`
Epic: `3 - Unify Review And Reveal Across Source And Graph`
Date: `2026-07-11`
Status: `completed`

## Epic Review

### Scope Reviewed

- `3.1` Feed accepted mutation outcomes into one semantic review model
- `3.2` Support bidirectional reveal across source, graph, and semantic SCM context
- `3.3` Publish the M8 proof corpus and verification path

### Outcome

Epic 3 closed the semantic coherence gap left after the mutation-path proofs.

The completed path is now:

- accepted source and graph mutations can publish one semantic review vocabulary
- source, graph, and semantic SCM can reveal the same subject through canonical semantic identity
- the M8 proof corpus is published with a repeatable verification path instead of only story-local evidence

### Review Verdict

No blocking implementation findings were identified in Epic 3 after the final proof-corpus pass.

Important scope note:

- the current source-originated path remains preview-first mutation evaluation and shared review or reveal coherence
- full source write-through execution into canonical runtime state is still later than M8

Verification evidence:

- `cmd /c "call java25 && .\gradlew.bat --no-daemon --console=plain :ide:lsp:test --tests com.engineeringood.athena.ide.lsp.AthenaSourceMutationRequestTest --tests com.engineeringood.athena.ide.lsp.AthenaProjectionRequestTest"`
- `yarn --cwd integrations/graph-glsp test`
- `yarn --cwd ide/theia-frontend test`
- `yarn --cwd ide verify:m8`

### What Worked Well

- Reusing `kernel/semantic-scm` kept review meaning semantic instead of interaction-specific.
- Canonical semantic identity remained the reveal anchor even when graph-local ids and projection-local refs existed downstream.
- Publishing the proof corpus made the milestone inspectable without replaying individual implementation stories.

### Challenges And Lessons

- Review unification is not enough by itself; reveal coherence matters because users judge semantic consistency through navigation behavior.
- Source-originated mutation wording needs care: normalization into the shared mutation-result model is not the same as full canonical write-through mutation.
- The proof corpus is part of the architecture. Without the usage path and verification entrypoints, the milestone would be much harder to trust.

### Technical Debt And Residual Risk

- The current source path still stops at governed evaluation and review output rather than canonical write-through.
- No automated desktop E2E flow yet proves the full human-facing review and reveal loop.
- Semantic SCM still proves milestone-level coherence, not final product-level approval workflow depth.

## Next Epic Preparation

### Carry-Forward Actions

1. Keep future source-authoring milestones explicit about whether they add preview, apply, or persisted command execution behavior.
2. Preserve canonical semantic identity as the only cross-surface reveal anchor even if richer graph frameworks are introduced later.

## Summary

Epic 3 successfully closed M8 as a coherent mutation milestone:

- unified review model proven
- bidirectional reveal proven
- proof corpus and verification path published

This epic is ready to mark `done`.
