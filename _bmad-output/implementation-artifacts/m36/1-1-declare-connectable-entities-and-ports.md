---
baseline_commit: 593d0a1dc86c0bac126fbde0501cc07051180346
---

# Story M36-E1.S1: Declare Connectable Entities And Ports

Status: done

## Story

As an engineer or AI agent,
I want to declare generic connectable entities, interfaces, and typed ports in Athena source,
So that connectivity has an explicit, type-safe semantic owner.

**Requirements:** FR-1, FR-4.

## Acceptance Criteria

1. Valid declarations compile into stable identities, types, compatibility parameters, source spans,
   and provenance.
2. `ConnectableEntity`, `Interface`, `Port`, `Connection`, and `Network` remain generic engineering
   vocabulary.
3. Existing M14 component knowledge remains upstream; this story does not introduce an ECS.
4. Invalid declarations publish the same source-spanned compiler and LSP diagnostics.

## Dev Notes

- Athena source is SSOT. Do not make SVG, renderer state, XML, or LSP transport authoritative.
- Use ANTLR/compiler contracts as parser authority; Tree-sitter is editor support only.
- Read current component and connection models through CodeGraph before editing.
- Keep this contract narrow: identity, interfaces, ports, compatibility, and provenance.
- Start RED. Run Gradle verification sequentially. End with AC-to-evidence, encoding audit, and
  `git diff --check`.

## Tasks / Subtasks

- [x] Define the narrow generic Connectable Entity and Port contracts in `connection-model` (AC: 1, 2, 3)
  - [x] Add RED contract derivation and invalid-contract tests.
  - [x] Derive contracts only from canonical Engineering IR and source provenance.
- [x] Integrate admitted Connectable contract diagnostics through the compiler semantic validation path (AC: 1, 4)
  - [x] Add an M36 source admission test and verify normal compiler diagnostics.
  - [x] Prove existing non-admitted Ports do not acquire M36 diagnostics.
- [x] Publish and verify the resulting compiler diagnostics through LSP (AC: 4)
  - [x] Add or update focused LSP diagnostic coverage.
- [x] Run story evidence gate (AC: 1, 2, 3, 4)
  - [x] Run sequential targeted and regression tests, encoding audit, and diff check.
  - [x] Record AC-to-evidence, file list, and completion notes.

## Dev Agent Record

### Debug Log References

- RED: `:kernel:connection-model:test --tests ConnectableEntityContractCompilerTest` failed before the contract existed.
- GREEN: connection-model contract test, focused M36 compiler test, and `AthenaCompilerTest` passed.
- Evidence: `:kernel:connection-model:test --tests ConnectableEntityContractCompilerTest`, focused compiler/LSP tests, and root `test` all passed; root suite completed in 12m 48s on 2026-07-28.
- Quality: `tools/encoding-audit.ps1` and `git diff --check` passed.

### Completion Notes

- AC 1-3: `ConnectableEntityContractCompiler` derives only admitted `connectable enabled` Engineering IR components, ports, interfaces, compatibility values, and provenance without creating an ECS.
- AC 4: compiler validation emits normal source-spanned `CONNECTION` diagnostics and LSP publishes them through the existing diagnostics pipeline.
- Regression fixtures were migrated to the current Java-style package identity and source hierarchy; M14/M16 locks were regenerated by the compiler as v2 derived state.

### File List

- `kernel/connection-model/src/main/kotlin/com/engineeringood/athena/connection/ConnectableEntityContracts.kt`
- `kernel/connection-model/src/test/kotlin/com/engineeringood/athena/connection/ConnectableEntityContractCompilerTest.kt`
- `kernel/compiler/src/main/kotlin/com/engineeringood/athena/compiler/AthenaCompiler.kt`
- `kernel/compiler/src/main/kotlin/com/engineeringood/athena/compiler/AthenaCompilerCompilationSupport.kt`
- `kernel/compiler/src/main/kotlin/com/engineeringood/athena/compiler/CompilerModels.kt`
- `kernel/compiler/src/test/kotlin/com/engineeringood/athena/compiler/AthenaM36ConnectableEntityCompilationTest.kt`
- `ide/lsp/src/test/kotlin/com/engineeringood/athena/ide/lsp/AthenaDiagnosticsPublishingTest.kt`
- `examples/m14/siemens-proof-corpus/` and `examples/m16/semantic-reuse-proof/` package sources, manifests, and derived locks
- Runtime, repository, CLI, compiler, language, SCM, and usage fixtures updated for current package and source-unit contracts.

### Change Log

- 2026-07-28: Implemented and verified M36-E1.S1; repaired stale fixture assumptions exposed by full regression.

## References

- `_bmad-output/implementation-artifacts/m36/epics.md` - M36-E1.S1.
- `_bmad-output/planning-artifacts/prds/prd-Athena-2026-07-28-m36/prd.md` - FR-1, FR-4.
- `_bmad-output/planning-artifacts/architecture/architecture-Athena-2026-07-28-m36/ARCHITECTURE-SPINE.md` - AD-1 to AD-4.
