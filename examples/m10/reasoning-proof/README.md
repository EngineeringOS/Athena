# M10 Reasoning Proof

- `baseline/` holds the before-state repository.
- `current/` holds the changed repository.
- The default Athena LSP M10 path uses the deterministic proof provider to turn governed evidence into inspectable proposals.
