# M23 Sample Project

This is the openable Athena workspace for the M23 milestone.

Open this folder in the IDE to verify a real `.athena` layout block:

- `src/01-layout-hints.athena` - system-scoped layout hints for placement, alignment, and grouping

The source file uses admitted M23 syntax. It is not a preview-only `.mjs` scenario.
