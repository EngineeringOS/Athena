# M29 Sample Project

This sample proves the M29 semantic interaction runtime path without making Theia the semantic
authority.

It contains:

- governed nested-port source for reveal/navigation
- compatible electrical relationship subjects
- invalid relationship candidates
- empty semantic context for component creation preview and accept proof

Open `examples/m29/sample-project` as the workspace. The primary source files live in `src/`.
